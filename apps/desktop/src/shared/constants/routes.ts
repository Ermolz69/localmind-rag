export const routes = {
  dashboard: "/",
  buckets: "/buckets",
  documents: "/documents",
  search: "/search",
  notes: "/notes",
  chat: "/chat",
  settings: "/settings",
  diagnostics: "/diagnostics",
} as const;
