[package]
name = "localmind"
version = "0.1.0"
description = "Offline-first local RAG desktop app"
authors = ["localmind"]
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
tauri = "2"
