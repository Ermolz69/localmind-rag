{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for localmind.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
