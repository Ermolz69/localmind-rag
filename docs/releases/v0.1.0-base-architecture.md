# localmind v0.1.0 - Base Architecture

This first release is the architectural foundation for `localmind-rag`. The goal is to make the repository understandable, buildable, testable, and ready for the next implementation stages without pretending the full product is complete.

## What This Release Means

`v0.1.0` is a base platform release:

- The monorepo shape is defined.
- Backend projects and dependencies are initialized.
- Frontend desktop scaffold is initialized.
- Local runtime folders and SQLite persistence are initialized.
- CI checks are split and readable.
- Portable-preview packaging exists.
- Documentation explains how the system should interact.

It is the “good skeleton” release, not the finished Knowledge App.

## Current Component Interaction

```mermaid
flowchart TB
    User["User"] --> Shell["KnowledgeApp desktop shell<br/>Tauri window"]
    Shell --> React["React UI<br/>apps/desktop/src"]
    React --> ApiClient["Typed LocalApi client<br/>shared/api"]
    ApiClient --> LocalApi["KnowledgeApp.LocalApi<br/>local sidecar HTTP API"]

    subgraph LocalRuntime["Local machine runtime"]
        LocalApi --> Paths["AppPathProvider<br/>portable / OS app paths"]
        Paths --> RuntimeFolders["runtime/app<br/>data files indexes logs"]
        LocalApi --> DbContext["AppDbContext<br/>EF Core"]
        DbContext --> SQLite[("SQLite<br/>knowledge-app.db")]
        LocalApi --> FileStorage["IFileStorageService<br/>local original files"]
        FileStorage --> Files["runtime/app/files"]
        LocalApi --> Ingestion["IIngestionQueue<br/>IngestionJob skeleton"]
        Ingestion --> Jobs[("ingestion_jobs")]
        LocalApi --> RagPorts["RAG ports<br/>chunker embeddings vector search answer"]
        RagPorts --> VectorIndex["Local vector index<br/>exact search now, replaceable later"]
        LocalApi --> AiRuntime["IAiRuntimeManager<br/>Ollama / llama.cpp adapters later"]
        AiRuntime --> Models["runtime/ai/models"]
    end

    LocalApi --> SyncPorts["Sync ports<br/>outbox-first"]
    SyncPorts --> Outbox[("sync_outbox")]

    subgraph OptionalRemote["Optional remote sync"]
        SyncApi["KnowledgeApp.SyncApi"]
        RemoteDb[("PostgreSQL")]
        RemoteFiles["Remote file storage"]
    end

    SyncPorts -. online + authenticated .-> SyncApi
    SyncApi --> RemoteDb
    SyncApi --> RemoteFiles

    React -. forbidden .-> SQLite
    React -. forbidden .-> AiRuntime
    React -. forbidden .-> Files
    React -. hidden behind LocalApi .-> SyncApi
```

## Intended Runtime Flow

```mermaid
sequenceDiagram
    actor User
    participant Desktop as Tauri desktop shell
    participant UI as React UI
    participant API as LocalApi sidecar
    participant DB as SQLite
    participant FS as Local file storage
    participant AI as Local AI runtime
    participant Remote as SyncApi

    User->>Desktop: Start KnowledgeApp
    Desktop->>API: Start LocalApi sidecar
    API->>API: Create runtime folders
    API->>DB: Apply EF Core migrations
    API->>AI: Check runtime and model availability
    Desktop->>UI: Open app window
    UI->>API: GET /api/health
    API-->>UI: OK

    User->>UI: Upload document
    UI->>API: POST /api/documents/upload
    API->>FS: Store original file
    API->>DB: Create document + file + ingestion job
    API-->>UI: Document queued

    API->>AI: Generate embeddings later
    API->>DB: Save chunks and embeddings later

    User->>UI: Ask question
    UI->>API: POST /api/chats/{id}/messages
    API->>DB: Load sources and chat context
    API->>AI: Generate local answer later
    API-->>UI: Answer with sources

    alt Internet and auth available
        API->>Remote: Push sync_outbox and pull manifest
        Remote-->>API: Remote changes
        API->>DB: Update local sync state
    else Offline
        API->>DB: Keep local changes queued
        API-->>UI: Offline mode / sync paused
    end
```

## Build And Quality Gate Diagram

```mermaid
flowchart LR
    Push["Push / Pull Request"] --> Check["GitHub Check workflow"]

    Check --> BackendFormat["Backend format"]
    Check --> BackendBuild["Backend build"]
    Check --> BackendTests["Backend tests"]
    Check --> FrontendFormat["Frontend format"]
    Check --> FrontendLint["Frontend lint"]
    Check --> FrontendTypes["Frontend typecheck"]
    Check --> FrontendBuild["Frontend build"]
    Check --> ColorGuard["Frontend color guard"]
    Check --> Compose["Docker compose config"]

    BackendBuild --> Summary["Check summary"]
    BackendTests --> Summary
    FrontendBuild --> Summary
    ColorGuard --> Summary
    Compose --> Summary
```

## What Was Actually Implemented

- Solution and project scaffolding for backend layers.
- Central .NET package management and strict build settings.
- Local SQLite persistence with initial migration.
- Runtime path resolution for portable folders.
- LocalApi health and skeleton endpoint surface.
- Basic domain, contract, and application port types.
- Desktop UI shell with app navigation, pages, semantic theme tokens, and typed API client.
- CI workflows and portable-preview release workflow.
- Repository hygiene and Docker ignore rules.

## What Comes Next

1. Move LocalApi endpoint logic into Application use cases.
2. Implement real file upload flow in the desktop UI.
3. Implement document parsing and ingestion workers.
4. Implement chunking, embeddings, and replaceable vector index.
5. Implement local AI runtime manager for Ollama and llama.cpp.
6. Implement RAG answers with source references.
7. Implement notes and note links.
8. Implement remote sync auth, devices, manifest, upload/download, and conflict handling.
9. Wire full Tauri native portable packaging with `KnowledgeApp.exe`.
