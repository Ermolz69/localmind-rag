# Offline And Online Sync

Offline work is always local and writes sync operations to an outbox. When network and auth are available, sync uploads metadata/files and downloads remote manifest changes.
