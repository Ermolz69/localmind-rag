# UI Documentation

The UI documentation is generated from the desktop Storybook build and copied into the documentation site during the docs workflow.

The generated Storybook site is available under `storybook/index.html` when the full docs workflow has produced the final site artifact.
