# Architecture

localmind is a portable offline-first desktop application built from Tauri + React, a self-contained local ASP.NET Core API sidecar, SQLite, local file storage, local vector search, and a local AI runtime adapter.
